services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ekoky
      POSTGRES_PASSWORD: ekoky
      POSTGRES_DB: ekoky
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
